---
description: Speak the newest listen.sh transcript aloud
allowed-tools: Bash(listen:*)
argument-hint: "[-a whole session | -s stop]"
---

!`listen -b $ARGUMENTS`

The transcript is now playing aloud. Say only which file is playing and roughly
how long it runs, in one line. Do not summarise or quote the transcript itself.
